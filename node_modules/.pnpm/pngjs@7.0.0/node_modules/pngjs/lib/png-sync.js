"use strict";

let parse = require("./parser-sync");
let pack = require("./packer-sync");

exports.read = function  read(buffer, options) {
  return parse(buffer, options || {});
};

exports.write = function  write(png, options) {
  return pack(png, options);
};
